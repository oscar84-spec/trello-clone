import { MdOutlineBackupTable } from "react-icons/md";

const TabIcon = ({ coloText = "text-text-light" }) => {
  return <MdOutlineBackupTable className={`size-6 ${coloText}`} />;
};

export default TabIcon;
